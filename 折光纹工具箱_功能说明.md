# 折光纹工具箱｜Photoshop 分层折光纹制作工具

折光纹工具箱（Refraction pattern master）是一套面向分层插画、卡牌、包装与装饰图案的 Photoshop 2020 折光纹制作脚本。打开源 PSD 后，可按图层选择纹样、调整线宽与疏密，并生成独立的透明折光纹母版。制作结果保留为可编辑的黑色形状图层，方便检查图层对应关系和后续分版。

## 主要功能

- **逐层分配。** 界面列出源 PSD 图层；可逐层启用、跳过、选纹样及设置参数。列表顺序即生成顺序，采用**子层优先**（组内子层先列，组容器排在其子层之后）。隐藏层和疑似整图合成层默认跳过，可人工启用；命名带“不加折光”等标记的层照常生成、输出组隐藏（见下）。
- **避免重复生成（默认开启）。** 同一区域被多层覆盖时，按生成顺序累加“已生成区域”，每层只生成尚未被占用的部分，避免同一片区域叠多套纹样变成实黑。细节纹样（子层）优先，组容器与背景层只补空隙，纯容器组会自动跳过（报告记为 `COVERED`）。区域裁剪仍是矢量路径，未使用像素蒙版。可在界面勾选关闭。
- **纹样建议。** 根据图层名称及区域形状提示适合的纹样和方向，可应用到当前层或全部图层。建议由本地规则生成，最终分配由使用者确认。
- **多种纹样。** GUI 当前提供 32 个纹样键，涵盖平行与流线、波纹与鱼骨、轮廓与放射、同心与螺旋、网格、鳞片、短线及短曲线等。下拉菜单里显示**中文名**（如折面平行纹、三角菱格纹、辐射摩尔纹），图层列表行里显示「中文名(键名)」；不同纹样的效果和适用区域可查阅 `references/design-logic.md`；其中部分纹样是简化或近似实现。
- **毫米参数。** 可逐层设置线宽、密/中/疏三档净隙、方向、振幅、波长、短线长度、中心位置和轮廓取样容差；高级参数栏支持 `key=value`。可将当前层的线宽及三档间隙应用到全部已启用层。
- **不折光命名（模糊识别）。** 图层或编组命名里带「不加折光 / 不折光 / 不做折光 / 无需折光 / 不做 / 不用 / 跳过 / skip」等字样（简写与中英文均可），该层及其全部子项仍会**照常生成**，但生成的输出组自动设为**隐藏**，使母版与源文件的层级和可见性对应。标记只影响可见性：这类层仍按原优先度参与「避免重复生成」（先扣除已占用区域，再占用自己生成的区域），所以「不折光」区域在成品里保持无折光纹。
- **分层矢量母版。** 每个生成区域对应一个 `ZG_OUT__` 组；组内为纯黑形状图层，以组级矢量蒙版约束到源图层取样区域。母版为透明背景、同画布尺寸的 PSD，源 PSD 不被覆盖。
- **输出组父子级。** 生成完成后按源图层层级把各 `ZG_OUT__` 组嵌进对应父组，只建立父子关系，不建立剪辑蒙版。若某层下面还有要生成的子层，该层的纹样会放进同名的「本层纹样」子组（避免容器蒙版把子层裁掉），叶子层仍是单层结构。
- **逐组导出。** 使用独立的 `export_groups.jsx` 脚本，递归收集母版里所有 `ZG_OUT__` 纹样组，分别导出为同画布、透明背景的 PSD 和 PNG，并生成导出清单；按不折光命名隐藏的组不导出，另记入 `skipped_hidden_groups.txt`。PNG 便于预览与交付，PSD 保留形状编辑能力。
- **大路径分批处理。** 形状构建按路径数和节点数分批（每批 ≤900 条子路径、≤60000 节点），并写入逐批进度与耗时。核心会自动把超过 Photoshop 单子路径 1000 锚点上限的几何压缩到上限内，不截断纹样；超出安全预算时明确报错。单层失败只跳过该层并记入报告，其余层继续生成。

## 可选纹样

| 类别 | GUI 中的纹样键 | 常见用途 |
|---|---|---|
| 方向与随形 | `parallel`、`facet`、`flow`、`wave`、`zigzag`、`contour`、`topographic` | 背景、折面、飘带、云、水面和轮廓层次 |
| 脊线与装饰 | `chevron`、`feather`、`herringbone`、`bilateral_flow`、`meander` | 叶脉、羽毛、织物、边饰 |
| 中心与旋转 | `radial`、`fan`、`cone`、`sunburst`、`concentric`、`ripple`、`spiral`、`vortex`、`petal_rosette`、`moire_radial` | 光环、徽章、水波、花心与焦点装饰；`moire_radial` 用于大面积背景/夜空的极光式反光 |
| 几何与短线 | `diamond_lattice`、`diamond_tri`、`triangle_lattice`、`hex_lattice`、`checker`、`carbon_fiber`、`scale`、`dash_field`、`dot_field`、`short_curve` | 硬质表面、网格、鳞片、过渡区域；`diamond_tri` 用于卡牌边框与边饰带 |

其中 **`diamond_tri`（三角菱格纹）** 由正倒三角咬合成菱形、菱形内含更细的内缩线条，菱形作为单元连续铺满区域，适合卡牌外框与边饰带；**`moire_radial`（辐射摩尔纹）** 由 2–3 组圆心微错位的放射线族叠加，线条交叉产生放射状摩尔条纹，压纹反光时呈现极光式流动感，适合大面积背景；未指定内径时中心用同间距同心圆环收口，不会留大片圆形空白。

**纹样建议**已按用途倾斜：名称含「边框/框/frame/边饰」的层优先建议 `diamond_tri`；名称含「背景/天空/bg/夜空」的层、以及占画布 60% 以上的大面积区域，优先建议 `moire_radial`。

设计资料中还收录编织、防伪曲线、复合光学变图等构想。这些**不在当前 GUI 的自动生成范围内**；请以界面下拉列表为可用纹样依据。

## 默认设置

| 参数 | 当前默认值 |
|---|---:|
| 常规线宽 | 0.15 mm |
| GUI 固定的最小线宽 | 0.10 mm |
| 密区 / 中区 / 疏区净隙 | 0.12 / 0.15 / 0.20 mm |
| 方向 | 0° |
| 短线长度 | 2 mm |
| 轮廓取样容差 | 1 px |

单元类纹样（回纹 `meander`、人字 `herringbone`、蜂巢 `hex_lattice`、碳纤维 `carbon_fiber`、鳞片 `scale`、点阵 `dot_field`）的格子尺寸由「线宽 + 中隙」推导，`波长mm` 只在合理区间内放大单元，避免出现「只有两三条线」的过疏效果；`checker` 的分格尺度仍由 `波长mm` 决定，但格内排线本身按基准间距排列。

“净隙”指相邻黑色线条轮廓之间的空白宽度，不等于两条线的中心距。实际局部宽度与间隔还需结合最终成品尺寸、纹样交会位置及工厂要求复核。默认值可在界面中按图层调整；最小线宽在 GUI 中固定为 0.10 mm。

## 使用方法

1. 在 Photoshop 2020 中打开分层 PSD。
2. 选择 **文件 → 脚本 → 浏览**，运行 `scripts/generate_refraction_gui.jsx`。
3. 在图层列表中逐层确认“生成折光纹”、纹样和参数；需要时使用“建议当前层”或“建议全部”。
4. 点击“生成”，选择输出目录。脚本会保存 `ZG_master_<时间戳>.psd`、`master_report_<时间戳>.txt`，并在生成过程中更新 `zg_progress.txt`。
5. 如需逐层文件，打开生成的母版，运行 `scripts/export_groups.jsx`，选择导出目录，取得各组 PSD、PNG 与 `export_manifest.csv`。
6. 交付生产前检查源层映射、透明背景、形状图层、区域蒙版，以及最终尺寸下的线宽、净隙和局部交会。

需要由外部工作流提供分配表时，也可使用 `scripts/generate_refraction.jsx` 读取 `job.json`。两种入口共用 `scripts/refraction_core.jsx`。

## 使用边界

源图层的透明度经 Photoshop 取样转为区域路径；取样容差会影响边缘细节和节点数量。细碎或深凹轮廓、复杂交叉纹样及大面积高密排线需要检查，必要时拆分区域处理。形状构建阶段的安全预算为单区域最多 **100,000 条路径、1,500,000 个节点**，单条路径最多 **900 个子路径、每个子路径最多 1000 个锚点**（Photoshop 硬上限，核心会自动压缩超限几何）；达到预算时会跳过该层并报错，不影响其余图层。预算在纹样几何生成之后检查，因此不能防止几何生成阶段的所有卡顿，也不是 Photoshop 对所有文件的性能保证。

区域取样（透明度→工作路径）原本逐点经 DOM 读取，实测为 O(n²)：1108 点约 74 秒、3731 点约 12 分钟，是整次生成耗时的主要来源。现改为优先用 ActionManager 一次读回整条路径（同样 3731 点约 0.4 秒），首次取样时与 DOM 结果逐点比对做标定，不一致会自动回退 DOM；`zg_progress.txt` 会写明每层实际使用的方式与分阶段耗时（2516×3756@900 下单层约 1.2–1.5 秒）。取样临时文档改为全程复用同一个，避免逐层新建/关闭文档在几十层后触发 Photoshop「没有足够空间停放新文档」导致整次失败。若仍遇到该提示，可在首选项→界面关闭「以选项卡方式打开文档」，或重启 Photoshop / 复位工作区。若仍需进一步缩短耗时，可适当放大「容差px」（节点更少）或按区域拆分生成。

脚本生成的是设计数据，屏幕效果不能直接代表最终材料上的折光效果。正式制作应根据加工方的线宽、净隙、材料、压纹方向和打样结果验收。当前大文件分批优化已通过代码级检查，仍需在具体 PSD 和 Photoshop 环境中测试耗时与稳定性。

---

适用环境：Adobe Photoshop 2020（ExtendScript/JSX）。工具目录中的 `工作流.md`、`references/` 与 `scripts/` 提供详细设计规则、参数说明及验收流程。
